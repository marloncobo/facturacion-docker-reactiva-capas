spring:
  sql:
    init:
      mode: always
  r2dbc:
    url: ${DATABASE_URL:r2dbc:mysql://db:3306/facturacion}
    username: ${DATABASE_USERNAME:user}
    password: ${DATABASE_PASSWORD:password}
  main:
    web-application-type: reactive

server:
  port: ${PORT:8080}